# DRISHTI-LENS Backend Dependencies

# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0

# Database
# In Docker (Python 3.11): asyncpg works via binary wheel
# On Windows Python 3.14 local dev: use psycopg2-binary + aiosqlite
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0          # local dev / testing (SQLite async)
psycopg2-binary==2.9.9     # local dev sync fallback

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# HTTP & Files
python-multipart==0.0.12
httpx==0.27.0
aiofiles==24.1.0

# ── Docker-only (add to Dockerfile.dev, not this file) ──────
# asyncpg==0.30.0   ← installed in Docker container (Python 3.11 slim)
