# DRISHTI-LENS Backend Dependencies

# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0

# Database
sqlalchemy[asyncio]==2.0.35
aiosqlite==0.20.0          # local dev / testing (SQLite async)
psycopg2-binary==2.9.9     # local dev sync fallback

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# HTTP & Files
python-multipart==0.0.12
httpx==0.27.0
aiofiles==24.1.0

# ── AI Pipeline (Phase 2) ─────────────────────────────────────────────────────
# Computer vision — image quality, segmentation, enhancement
opencv-python-headless==4.10.0.84

# Deep learning — DR grading model, Grad-CAM
# CPU build: safe on all platforms. GPU: replace with torch+cu118 wheel
torch==2.4.1
torchvision==0.19.1

# Scientific computing
scikit-image==0.24.0
scikit-learn==1.5.2        # calibration, validation metrics
scipy==1.14.1              # temperature scaling optimisation
Pillow==10.4.0

# ── Validation Pipeline (Phase 3) ────────────────────────────────────────────
# matplotlib required for ROC/confusion matrix plots
matplotlib==3.9.2
pandas==2.2.3

# ── Docker-only (add to Dockerfile.dev, not installed locally) ────────────────
# asyncpg==0.30.0   ← installed in Docker container (Python 3.11 slim)
